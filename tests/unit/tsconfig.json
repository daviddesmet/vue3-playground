{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // be explicit about types included
    // to avoid clashing with Jest types
    "types": ["@types/jest"]
  },
  "include": ["../../src/**/*.d.ts", "./**/*.ts"]
}
